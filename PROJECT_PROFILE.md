# Project Profile

This file records the current project's technical facts for Agent routing. It does not replace build configuration and must not contain credentials, tokens, proxy settings, or production URLs.

```text
backend_framework: spring-boot
build_tool: maven
backend_module: backend
frontend: vite-vue
frontend_module: frontend
frontend_language: typescript
frontend_state: pinia
frontend_router: vue-router
database: mysql
test_command: mvn test
```

The backend test command is run from `backend/`. The frontend build command is run from `frontend/` with `npm run build`.
