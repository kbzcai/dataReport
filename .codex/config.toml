# 1. 当前项目已授权本地开发与测试，减少重复审批等待。
approval_policy = "never"

# 2. 默认推理强度：保留多 Agent 协同，减少简单修改的单轮等待。
reasoning_effort = "medium"

# 3. 沙箱模式：当前项目需要本地构建、启动和网页回归。
sandbox_mode = "danger-full-access"

[sandbox_workspace_write]
network_access = true
